"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _login = _interopRequireDefault(require("../../actions/login/login"));

var _isNpx = require("../../util/isNpx");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

const commandPrefix = (0, _isNpx.prefixCommand)();
const helpText = `
Options
  --sso <slug> Authenticate against a third-party identity provider
  --provider <providerId> Authenticate against a specific provider
  --no-open Do not open a browser window to log in, only print URL

Examples
  # Login against the Sanity.io API
  ${commandPrefix} login

  # Login with SAML SSO
  ${commandPrefix} login --sso org-slug

  # Login with GitHub provider, but do not open a browser window automatically
  ${commandPrefix} login --provider github --no-open
`;
var _default = {
  name: 'login',
  signature: '[--sso <slug>]',
  description: 'Authenticates against the Sanity.io API (no flag) or a third-party identity provider (with --sso flag)',
  helpText,
  action: _login.default
};
exports.default = _default;